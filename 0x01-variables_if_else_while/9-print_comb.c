#include <stdio.h>
/**
 * main -this is the main function
 * Return: always 0
 */

int main(void)
{
	int n = 48;

	while (n <= 56)
	{
		putchar(n);
		n++;
		putchar(44);
	}
		putchar(57);
		putchar('\n');

	return (0);
}
