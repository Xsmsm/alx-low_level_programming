#include <stdio.h>
/**
 *main- main start
 *Return: always 0
 */
int main(void)
{
	int n = 96;
	int u = 65;

	while (n <= 120);
	{
		putchar(n);
		n++;
	}

	while (u <= 90);
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
